# TODO - Auto Exploratory Selenium Framework (Java)

## Phase 0: Repo understanding
- [x] Read existing Page Objects and tests.
- [x] Confirm stack: Maven + Selenium 4 + TestNG.

## Phase 1: Explorer/Recorder (crawl + record)
- [ ] Add new package: `com.ibmdemo.auto.*`
- [ ] Implement `SiteExplorer` that:
  - [ ] Loads base URL
  - [ ] Enumerates interactive elements (click/type)
  - [ ] Builds a navigation/state graph
  - [ ] Records element candidate locator strategies
  - [ ] Enforces crawl depth / max steps
- [ ] Implement `StateFingerprint` to avoid infinite loops.
- [ ] Implement `LocatorCandidate` model + locator inference heuristics.
- [ ] Persist exploration result to JSON (no extra deps if possible) OR as simple text/Java-serializable structures.

## Phase 2: Code Generator (generate Java Page Objects + TestNG tests)
- [ ] Implement `CodeGenerator` that:
  - [ ] Writes `src/main/java/com/ibmdemo/generated/pages/*` page objects with discovered locators
  - [ ] Writes `src/test/java/com/ibmdemo/generated/tests/*` TestNG tests for scenario paths
- [ ] Create `ExplorerRunner` entrypoint that runs exploration then generation.

## Phase 3: Test execution
- [ ] Add a TestNG test `AutoGeneratedExplorerTest` to invoke the runner.
- [ ] Run `mvn test` and `mvn -Dtest=AutoGeneratedExplorerTest test`.

## Phase 4: Iterate heuristics
- [ ] Tune element selection + locator scoring on SauceDemo.
- [ ] Add safety rails for form submission and destructive actions.

